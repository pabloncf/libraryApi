<h1>Edite seu livro: </h1>
<form action="/books/updatebook" method="POST" class="form" enctype="multipart/form-data">
    <input type="hidden" name="id" value="{{book.id}}">
    <div class="form-control">
        <label for="title">Título: </label>
        <input type="text" name="title" placeholder="Digite um título" value="{{book.title}}">
        
    </div>
    <div class="form-control">
        <label for="pageqty">Quantidade de Páginas: </label>
        <input type="number" name="pageqty" placeholder="Quantas páginas o livro tem?" value="{{book.pageqty}}">
    </div>
    <div class="form-control">
        <label for="image">Imagem do Livro: </label>
        <input type="file" name="image" accept="image/*">
        {{#if book.image_path}}
            <img src="{{book.image_path}}" alt="{{book.title}} capa">
        {{else}}
            <p>Imagem não disponível.</p>
        {{/if}}
    </div>
    <input type="submit" value="Editar">
</form>
<div class="accessAvaiableBooks">
    <a href="/books">Voltar para os livros disponíveis</a>
</div>